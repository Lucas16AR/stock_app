services:
  - type: web
    name: stock-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: stock-db
          property: connectionString

databases:
  - name: stock-db
    plan: free